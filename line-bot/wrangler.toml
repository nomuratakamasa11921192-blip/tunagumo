name = "tsunagumo-line-bot"
main = "src/index.js"
compatibility_date = "2025-01-01"

# wrangler kv namespace create tsunagumo_line_chat_history で作成したIDに置き換える
[[kv_namespaces]]
binding = "CHAT_HISTORY"
id = "2dd62f340c054f21a9358985d9e2d503"

[vars]
# 2026-09-16: AnthropicからOpenAIへ移行(SaaS本体と同じ提供元に揃えた)
OPENAI_MODEL = "gpt-5.6-terra"
GOOGLE_CALENDAR_ID = "primary"
# SaaS本体(saas/)のデプロイ先URL。未デプロイの間は空のままでよい
# (トライアル開始自体は失敗しない。SaaS側のテナント発行だけスキップされる)。
SAAS_BASE_URL = "https://app.tunagumo.com"
# トライアル終了後に課金するStripeのprice ID(live mode、2026-09-16に料金ページの3プランへ
# 合わせて作成)。秘密情報ではないためここに直接書いてよい。
# SaaS側のプラン名(src/core/ai_budget.py)との対応: light / standard / unlimited(=プレミアム)
STRIPE_PRICE_LIGHT = "price_1UGHevGj5LbAzw5NEfkaCyXF"      # ライト 39,800円/月
STRIPE_PRICE_STANDARD = "price_1UGHgqGj5LbAzw5NHgFjODed"   # スタンダード 98,000円/月
STRIPE_PRICE_PREMIUM = "price_1UGHgBGj5LbAzw5NM3fIrpUc"    # プレミアム 198,000円/月
# 2026-09-25 会社プラン。Stripe APIでJPY・月額・内税・有効を照合済み。
STRIPE_PRICE_BASIC = "price_1UJZS7Gj5LbAzw5N48iWzNz7"     # ベーシック 税込39,800円/月/社
STRIPE_PRICE_BUSINESS = "price_1UJZTjGj5LbAzw5NoElecOWp"  # ビジネス 税込59,800円/月/社

# 以下はここに書かない。wrangler secret put で個別に設定する:
#   LINE_CHANNEL_SECRET
#   LINE_CHANNEL_ACCESS_TOKEN
#   OPENAI_API_KEY       (2026-09-16にANTHROPIC_API_KEYから変更。wrangler secret put OPENAI_API_KEY)
#   GOOGLE_OAUTH_CLIENT_ID
#   GOOGLE_OAUTH_CLIENT_SECRET
#   GOOGLE_CALENDAR_REFRESH_TOKEN
#   ZOOM_PERSONAL_ROOM_URL
#   SAAS_ADMIN_API_KEY   (saas/.envのADMIN_API_KEYと同じ値)
#   STRIPE_SECRET_KEY    (Stripe本番化時は sk_live_... に必ず差し替える。sk_test_のままだと課金されない)
