# 本番反映の実施記録（2026-09-18、Codex）

ユーザーの明示指示「残ってるのやって本番にして」に基づき、さくらVPSの既存本番へ反映した。通常開発の本番禁止を解除した恒久運用ではない。

## 反映したコードと検証

- 反映コミット：`0dacb8fc0b2bbde8988fa60327bb1cb1f521c3ec`。入力原文・追加回答の引き継ぎ修正、従来VPS上の変更、画像編集APIの修正を含む。
- PCの開発用Docker：変更前651件成功。画像APIの未対応パラメータを再現するテストは修正前に失敗し、修正後は **652件成功、失敗・スキップ0件**（101.58秒、警告1855件）。画面11件成功（画像クライアント修正前。画面コードはその後変更なし）。
- 日次QA24件・営業LINEボット3経路は以前の成功を維持。今回この2つのコードは変更していない。
- VPSの独立した取得先：`/home/ubuntu/releases/release-408aa64`。既存のVPS日次QA用cloneは触っていない。
- Gitのsaas/website追跡ファイル283件をtar化。PCとLinuxの改行差を正規化したファイル名・SHA256一覧のハッシュが双方で一致：`e59ff42702881990c75e86b2a8c2a96ca37aa797d1a061f1a20abf39685ec19a`。
- ENV実値・秘密鍵・private・DBバックアップを含めず、パストラバーサルとリンクも検査した。既存ENV2箇所とworkspaceを除外して本番へ展開し、展開後の全対象ファイルのハッシュも照合した。
- 本番Composeは稼働コンテナのラベルで **docker** と確認。`/opt/tsunagumo/saas/docker` の既存2つのComposeファイルと変数展開用ENVを明示し、API起動確認後にschedulerを更新した。イメージは `tsunagumo-release:0dacb8f` を `docker-api:latest` / `docker-scheduler:latest` へ付与。更新処理31.2秒。
- DBは再起動・初期化せず、schema0024と既存5テナントを保持。今回migrationの追加はない。

## 復旧した本番設定

- 本番ENVからTENANT_SECRET_KEYが欠落していた。VPS内の既存バックアップにある元の鍵で、DBの非空の暗号化フィールド6件すべてを復号できることを確認し、同じ鍵を復旧した。鍵の新規生成・ローテーション、暗号化済みデータの書換えはしていない。
- VPS開発用の既存設定にあるREINFOLIB_API_KEYを本番ENVへ設定した。再作成後のAPIから埼玉県の市区町村74件を実取得できた。
- `/opt/tsunagumo/saas/workspace` がroot所有で、APIのuid1000では生成画像を保存できなかった。ディレクトリ自体の所有者を1000:1000へ修復し、作成・削除の検査を行った。既存ファイルの一括所有者変更はしていない。
- 機密ENVはVPS内に保持し0600。値の表示・Git追加・PCへの転送はしていない。

## 画像の実API確認

- 架空の空室画像の生成成功：usageからの計算額0.005 USD。
- 続く編集は`invalid_input_fidelity_model`でHTTP400。`gpt-image-2.5-sunburst`へ送っていた`input_fidelity`を取り除き、回帰テストを追加した。
- 修正版イメージで同じ架空画像にソファ・テーブルを追加する編集が成功。保存したJPEGの存在とHTTP200配信を確認、usage計算額0.053663 USD。
- 初回の保存権限エラーでもAPI側の生成は行われたため、上記の金額は検証全体の総請求額ではない。画像の目視による品質確認はPCのHTTPS経路制限で未実施。

## 本番反映後の確認

- API起動時のDB/文章APIチェックを通過し、VPSから本番HTTPSが200。
- `/`、`/admin.html`、生成画像が200。未認証の`/api/account/usage`と`/api/sessions`は401、`/openapi.json`は404。
- 既存管理者キーによる`/admin/dashboard`は200、テナント件数5。キーと顧客情報は出力していない。
- 復旧した鍵で既存の暗号化フィールド6件が復号でき、再起動後も不動産APIの実データを取得できる。
- 公開サイトのHTML全13ページをHTTPSで取得し、200と本番配置ファイルの内容ハッシュ一致を確認。
- 更新開始から912秒まで監視。定期取得27回すべてHTTPS200、API/scheduler/DBは稼働継続・再起動0回。APIとschedulerの更新後ログはERROR/Traceback・WARNINGとも0件。HTTPS取得とコンテナ状態収集を合わせた計測時間は最大191ms。実行ログと `final-result.json` はVPSの下記バックアップディレクトリ内に保存。

## 通知メールのDNS修復

- Resendに登録済みのtunagumo.comはfailed。既存DKIMはverified、sendのTXTは設定済みだが、MXが欠落していた。
- さくらドメイン管理で **send MX 10 feedback-smtp.ap-northeast-1.amazonses.com.** の1件だけを追加。既存のルートMX、A、SPF、DKIM等は保持した。SOAは2026091801へ更新、権威DNSでもMXを確認した。
- Resendの既存ドメインの再認証を開始（API HTTP200）。その後、Resend APIで **verified** を確認。メールは送信していない。
- RESEND_FROM_EMAILは本番で未設定。既存の通知先3件・承認待ち1件があるため、送信元・テスト宛先が確定するまで未設定を維持する。

## バックアップと切り戻し

VPS内の `/root/tsunagumo-release-20260918`（0700）に保存。外部には転送していない。

- `db.dump.enc`：pg_dumpのカスタム形式を既存のバックアップ用パスフレーズでAES256CBC/PBKDF2暗号化。暗号化前にpg_restore --listが成功し、復号後のバイト列ハッシュ一致を確認。隔離したネットワークなし・tmpfsの一時DBへ実復元し、schema0024と5テナントを確認。一時コンテナは検証後に削除した。
- `db-globals.sql.enc`：クラスタ共通のロールも同じ方式で暗号化バックアップし、復号一致と実適用を確認。空のクラスタにはapp_roleがないためDB単体では復元が失敗する。所有ロールtsunagumoで初期化し、そのCREATE ROLE文だけを既存として省略してglobalsを先に適用、その後pg_restore --exit-on-errorで復元した。
- このリリースのDBバックアップはカスタム形式。通常の `saas/docker/restore_db.sh` が受け付けるgzip圧縮SQLとは形式が違うため、そのスクリプトへ直接渡さない。実検証手順はVPS内の `restore_check.py` を参照。
- `previous-code.tar.gz`：更新前のsaas/website、既存ENVを含む。workspaceと既存バックアップ群は除外。tar一覧検査済み。
- `Caddyfile.before`：更新前のCaddy設定。今回Caddy設定は変更していない。
- 旧稼働イメージはイメージオブジェクトが既に失われていたため、稼働中APIとschedulerを停止せずsnapshotを保存：`tsunagumo-rollback-api:20260918` / `tsunagumo-rollback-scheduler:20260918`。DBについても停止なしで `tsunagumo-rollback-db:20260918` を保存し隔離復元に利用した。これらはENVを含むためVPS内限定。
- `deploy.py` / `deploy.log`：展開・照合・API→scheduler更新の記録。API起動失敗時は旧コードとsnapshotへ戻す処理を用意。今回は切り戻し不要。
- 手動で戻す場合は既存ENV2箇所とworkspaceを除外して旧コードを展開し、snapshotをdocker-api/docker-schedulerへ再タグ付けしたうえで、同じ本番Compose指定でapi/schedulerのみ再作成する。DBのダウングレード・ボリューム削除はしない。復旧済み暗号鍵を古い欠落ENVで上書きしない。

## 接続と未完了項目

- SSH22/2222とIPv6はPCから接続できず、ユーザーがログインしたさくらシリアルコンソールで実施。SSHサービスと待受は正常、既存パケットフィルターの80/443/22/2222許可設定は変更していない。
- PCのHTTPSはFortinetの証明書チェーン検証で失敗する。VPSからは証明書検証を有効にしたHTTPSが正常。PC側のブラウザー実画面確認は残る。証明書検証無効化や警告回避は行っていない。
- 本番StripeキーはHTTP401。VPSバックアップに別の有効キーは見つからなかった。PCの既存キーは有効だが、ブラウザーのStripeアカウントとは別アカウント。取り違えを避けて書換えず、利用アカウントをユーザーに確認中。請求ポータルの解約設定照合と決済疎通は未完了。
- SaaS用LINE登録0件、受信メールアカウント0件。テスト対象のLINE公式アカウント名と返信用メールアドレスをユーザーに確認中。営業LINEのWebhookは変更していない。
- アプリの本番反映は実施済みだが、決済・通知・LINE/メール実送受信まで完了した状態ではない。
