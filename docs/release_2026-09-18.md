# 本番反映の準備と実施状況（2026-09-18、Codex）

ユーザーは今回「残ってるのやって本番にして」と明示した。本番反映の再承認を求める段階ではなく、接続の復旧と未確認項目の検証を進める段階。**この記録の時点では本番へ転送・展開・再起動していない。**

## 検証済みの反映候補

- ソース：`408aa642e94a49d8a769448823db9d4fd19ffa3c`。最新mainのQA記録とPCの入力情報欠落修正を含む。
- PCで開発用Dockerを再ビルドし、バックエンド **651件成功、失敗・スキップ0件**（173.43秒、警告1855件）。画面テスト **11件成功**（26.347秒）。本番テストではない。
- 前回の検証済み日次QA24件、営業LINEボット3経路はコード変更なしのため今回再実行していない。
- Gitの追跡ファイルのみから `saas/` と `website/` を梱包。保存先はGit対象外の `saas/workspace/pc-verification/release-408aa64.tar.gz`。
- 312エントリ、39,054,592バイト。SHA-256：`77e39b72666a00148de97a299aae750a0352c814660e801b87a0504595f5d47e`。
- ENV実値・private・DBバックアップ・生成物・秘密鍵・危険な展開パス・リンクの混入を検査した。テスト内の明示的な架空キー3種は実キーと区別。追跡されたENVテンプレートと空のworkspace用 `.gitkeep` は含む。
- 対象の主要変更は、原文・追加回答の全工程への引き継ぎ、画面遷移中の応答保護、利用量表示、ポータルメールの連絡先抽出、公開文面・緊急案内の修正。最新の本番配置ファイルとの差分は接続後に確認する。

## 接続について確認できたこと

- さくらVPS管理画面では対象サーバーが稼働中で、既存のIPv4が一致。
- 既存パケットフィルターはHTTP 80、HTTPS 443、SSH 22、追加のTCP 2222を許可している。設定は変更していない。
- PCから22番・2222番のSSHがタイムアウト。IPv6も接続できなかった。
- 管理画面にユーザーがログイン後、シリアルコンソールを開き、OSの `ubuntu` ログイン／パスワード入力待ちまで確認。入力待ちは60秒でタイムアウトし、現在は `login:` に戻っている。OSパスワードは取得していない。
- 本番URLへのHTTPS接続はPCのPython・PowerShell・Chromeで証明書検証が失敗。証明書のメタデータのみを調べると、発行者はFortinetのネットワーク機器だった。PC経路の検査・制限の可能性があり、これだけで本番Caddyの証明書不具合とは断定しない。
- 証明書検証を無効化したアプリ通信やChromeの警告回避はしていない。ローカルの証明書ストア・VPSのファイアウォール・SSH設定も変更していない。
- PCの通知送信元は `resend.dev` のテスト用ドメイン。今回のResendドメイン一覧取得はHTTP 403で、送信認証や本番設定の有効性は未確定。メール送信はしていない。

## 接続後の実施順序

1. OSコンソールで現在のSSHサービス、待受ポート、接続ログを読み取り、PC側のネットワーク制限かサーバー側の障害かを確認する。不要な全開放・認証方式変更はしない。
2. VPS開発cloneの未保存変更と設定を確認。本番の稼働コンテナから既存Composeプロジェクト名・使用ファイル・イメージIDを特定する。秘密情報を出力する `docker inspect` 全体表示や `docker compose config` 全体表示はしない。
3. 現行コードと反映候補の差分、DBのmigration番号、空き容量、API・schedulerの状態を確認。コード内の運用変更を保存し、既存の2つのENV、workspace、DBボリュームを保持する。
4. OpenAI画像の実機確認、必要なLINE・メール設定の有無、通知元ドメインの状態を確認。実送受信は対象アカウントと宛先が確定したテストに限定する。営業LINEのWebhookをSaaS用へ転用しない。
5. 現行のコード・設定・イメージ参照を復旧用に保管し、DBのバックアップを作成・検証。実設定のバックアップはVPS内の権限制限された場所に保持し、PCやGitHubへ転送しない。
6. 検証済みtarをVPSへ転送しSHA-256を照合。転送・展開の対象を `saas/` と `website/` に限定し、既存ENV・workspaceを上書きしない。Caddy設定やDBサービスを根拠なく変更しない。
7. 確認した本番Compose名と `docker-compose.yml`・`docker-compose.prod.yml` を明示してビルド。必要なmigrationを確認してから適用し、APIを再作成する。起動確認後にschedulerを更新する。DBボリューム削除・初期化はしない。
8. 本番の起動時ヘルスチェック、未認証APIの401、公開画面、利用量表示、文章生成、画像経路、通知・schedulerの状態を確認。アプリ起動後15分のエラー・応答時間を確認する。開発テスト成功だけで本番反映成功とはしない。

## 復旧条件

APIが起動しない／502が続く、認証済みの基本操作が失敗する、tenant分離・予算計上に異常がある場合は、新しいschedulerを止めて更新前のコード・イメージと既存設定へ戻す。DBの自動ダウングレードはしない。必要なDB復元はバックアップの検証結果とschema差分を確認したうえで判断する。

## 未完了

VPSのOSログイン、SSH復旧、現在の本番・開発設定の直接確認、OpenAI画像の実生成、SaaSのLINE・メール実送受信、バックアップ、本番転送・反映・反映後確認は未完了。法務・委託契約の最終判断や実データの網羅性も、テスト成功を根拠に完了扱いしない。
