# 引き継ぎメモ（2026-09-15〜17 のClaude Code作業分）

Codexが続きを進めるための状態まとめ。詳細な経緯は `CHANGES.log` の2026-09-15以降を参照。

---

## 1. いま本番で動いているもの

| 項目 | 状態 |
|---|---|
| SaaS本体 | `https://app.tunagumo.com`（`/opt/tsunagumo/saas`、docker compose） |
| 営業サイト | `https://tunagumo.com`（`/opt/tsunagumo/website`、Caddyで静的配信） |
| 営業LINEボット | Cloudflare Worker `tsunagumo-line-bot`（`line-bot/`） |
| 文章生成 | **Anthropic（Claude）**。`LLM_PROVIDER=anthropic` |
| 画像生成・音声合成・文字起こし・資料検索 | OpenAI（Claudeに同等機能が無いため固定） |
| DBマイグレーション | 0024まで適用済み |

### 直近で追加した主な機能
- **問い合わせの24時間一次受け**（Webチャット・LINE・メール）。緊急（火災/ガス/水漏れ等）は
  AIを使わない定型の安全案内を自動返信し、担当者へ【緊急】通知。`src/agent/inquiry_triage.py`
- **メール即レス**（IMAP受信→AIが回答→SMTP返信）。`src/channels/mail.py` `src/agent/mail_scan.py`
- **物件提案・追客メール**（同意管理・法定表示・配信停止つき）。`src/core/proposals.py` `src/agent/proposal_scan.py`
- **文章生成の提供元切り替え**（`LLM_PROVIDER`でClaude/OpenAI。`src/agent/llm.py`）

---

## 2. 運用上、踏んではいけない地雷（今回実際に踏んだもの）

1. **`.env`を変えたら `docker compose restart` では反映されない。** 必ず `up -d`。
   restartはコンテナを作り直さないため、古い環境変数のまま起動して本番が502で止まる。
2. **モデル名はAPIのID表記で書く。** 通称 `claude-haiku-4.5` は404になる。
   正: `claude-haiku-4-5-20251001`。`config/*.yaml` の単価表のキーも同じ表記に揃えること
   （ずれると、そのモデルのコストが**0円として記録**され予算管理が狂う）。
3. **APIキーをチャットに貼らない。** 貼った場合は即座に無効化と再発行が必要。
4. 開発用cloneで `docker compose` を使うときは `-p tunagumo-dev`（`SYSTEM_PROMPT.md` §6.1）。

---

## 3. 残っている作業（優先度順）

### A. 人の操作が必要
1. **LINE・メールの実機設定とテスト** → 手順は `docs/setup_line_mail.md`
   （Webhook設定、応答メッセージのオフ、Gmailはアプリパスワード）
2. **不動産情報ライブラリのAPIキー**（2026-09-03に承認済みだがキー記載メールが届かず、
   9/15に事務局へ再送依頼済み）。届いたら `REINFOLIB_API_KEY` を設定し、XIT001/XIT002/XKT013を
   実データで確認する（`src/core/reinfolib_client.py` は公式マニュアル準拠に書き直し済みだが未検証）。
3. **規約・プライバシーポリシー・緊急時の自動応答文面の最終確認**（法務・安全に関わる文言）。

### B. 未検証（実データ・実機での確認待ち）
- Claude切り替え後の文章品質と、コスト記録が正しく増えるか（「設定・利用状況」で確認）
- 画像編集（バーチャルステージング）の実用品質
- LINE/メールの実送受信（フェイクでの検証は済み）
- `ESTIMATED_OPENAI_IMAGE_COST_USD`（画像1枚のフォールバック単価）は実測後に補正が必要

### C. 未着手・調査中
- **「依頼の処理中に新しい操作ができない」** とユーザーから報告あり（2026-09-17）。再現条件が未特定。
  UI側のポーリング（`frontend/index.html` の `pollTimer`）か、`/api/sessions` の処理待ちが原因の可能性。
  まず再現手順を確認すること。
- ポータル反響メールからの連絡先抽出は簡易実装（本文のパターン依存）。実際の反響メールで要調整。
- `plan_limits.py`（プラン別の動画本数上限）はどこからも呼ばれていない。実装するか削除するか要判断。

---

## 4. テストの動かし方

本番と同じDockerで動かすのが基本（PGroongaを含むため）。
```
cd /opt/tsunagumo/saas/docker   # 本番。開発用cloneなら -p tunagumo-dev を付ける
docker compose exec api pytest -v
```
- 2026-09-17時点で **586件成功**。
- 開発用cloneの簡易環境（埋め込みPostgres）では、ffmpeg・PGroonga不在により**22件が環境要因で失敗**する。
  この22件は既知で、コードの問題ではない。
- 営業LINEボットのテストは `cd line-bot/test && node bot_test.mjs`。
