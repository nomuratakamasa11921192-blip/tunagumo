# 追加の本番反映記録（2026-09-19、Codex）

ユーザーの「良いよ全部やって終わらせて」という、準備済みStripeキー発行・残る本番設定作業への承認を受けて実施した。通常開発での本番禁止を恒久的に解除するものではない。

## 完了した本番変更

- 反映コード：`04de792947024abc24a5217ced26bf954e86b02c`。前回本番 `0dacb8f` との差分はHiggsfieldクライアントと回帰テストの2ファイル。今回新たなアプリコード変更はない。
- PCで事前に全662件成功（110.59秒、警告1,855件）。その同一内容をVPSでビルドし、API→schedulerの順に反映した。更新処理27.8秒。
- 本番イメージ：`tsunagumo-release:04de792`、ID `sha256:1077bedf4d96a06cb6b7771d95e7eee60865ebfa6036721f4fa1287cb0fbd901`。
- 既存Stripe Webhookの署名シークレットを `STRIPE_WEBHOOK_SECRET` に設定。Stripe側のシークレット自体は変更していない。取得・入力・保存で秘密値をログやチャットに表示せず、VPSのroot専用ディレクトリに0600で保存した。
- `RESEND_FROM_EMAIL=notifications@tunagumo.com` を設定。`ADMIN_NOTIFICATION_EMAIL` は指定先と既に一致していたことを確認して維持。`SAAS_PUBLIC_URL` は既存値があれば維持する処理を使用。
- `TENANT_SECRET_KEY` とCompose用DB接続ENVの一致を確認して保持。DBを再作成・再起動せず、schema0024・既存5テナントを保持した。

## 実機検証

- 公開アプリ、管理画面、公開サイトがHTTPS200。更新後210秒と368秒の確認でAPI・scheduler・DBの稼働継続、再起動0回、API/schedulerのERROR・Traceback・WARNINGは0件。
- 本番Webhookに、金銭処理を伴わない専用イベント `evt_codex_integration_20260919` / `codex.integration_check` を送信。正しい署名でHTTP200、同一イベントの再送は `duplicate=true`、不正署名はHTTP400。
- 検証前後で全テナントの契約有効状態・追加AI予算が不変。Webhook受信履歴に検証イベント1件を記録した。Stripeによる実課金・購入完了イベントの実配信は未実施。
- Resendで `tunagumo.com` のverifiedを再確認。送信元有効化前に、期限リマインドと資料期限通知の送信待ち件数がともに0であることを確認した。
- ユーザー指定の本人宛てに接続確認メールを1件送信し、ResendのHTTP200と **delivered** を確認。冪等キーを付けて重複送信を防止し、配達記録はVPS保護フォルダに保存した。他の宛先へのテスト送信はしていない。

## Higgsfieldの実接続結果

本番DBにある本人テナントのHiggsfieldキーを、本番の暗号鍵で復号して使用した。無効な入力はモデルAPIがHTTP422で拒否し、実際の画像生成は **`not_enough_credits`** で拒否された。画像生成の完了・動画生成・画像編集は未検証。

結果は `/opt/tsunagumo/saas/workspace/higgsfield-integration-20260919.json` に保存。APIへの接続とクライアント修正の反映は完了したが、生成機能の稼働にはAPI側の残高が必要。追加購入・自動チャージ設定は行っていない。

[Higgsfield公式説明](https://higgsfield.ai/creator-hub/help-center/integrations/what-is-the-higgsfield-api)によると、Webサイトの月額契約・クレジットと、SaaS組み込み用APIの米ドル残高は別枠。MCP/CLIはWebプランのクレジットを使うため、この違いを引き継ぎ時にも維持する。

API管理画面へはログイン済みだが初回プロフィール設定で停止している。「自社利用」「Model APIs」まで選択済み。連絡先・役職・人数は未入力で、新規APIキー・支払い方法は登録していない。

## Stripeの残件

> **【2026-09-21 訂正】この節の記述は古い。** キーは09/20時点で発行済みであり、
> 「本人確認が未完了のため未実施」は誤り。2026-09-21にローテーションのうえ本番へ
> 反映し、疎通200と4権限を確認済み。**この節の手順をやり直す必要はない。**
> 最新は [stripe_key_2026-09-21.md](stripe_key_2026-09-21.md) を参照。

- 準備済み制限付きキー `tsunagumo-saas-production-20260919` の発行はユーザー承認済み。発行ボタンを押すとStripeが追加の本人確認を要求したため、パスキー確認を開始し、ユーザーへ操作を依頼した。
- 本人確認が未完了のため、新しいAPIキーの取得・設定は未実施。現行本番キーで既存ライト価格をGETするとHTTP401。このキーに依存する追加購入・請求ポータル作成の復旧は完了していない。
- Webhook署名キーの設定と受信検証は上記のとおり完了済み。Webhook設定をやり直す必要はない。
- 再開時は同じ発行画面・同じ権限で本人確認を完了してキーを保護保存し、既存価格IDで対象アカウントを照合する。新規商品・重複キー・既存キー失効は不要。発行済みかを確認してから再送操作を行う。

## 回収・バックアップと切り戻し

- 前日依頼された1,994ファイルのPC回収は完了済み。今回その再転送はしていない。
- SSH22番は引き続きPCからタイムアウト。ユーザーがログインしたさくら管理画面から既存rootシリアルコンソールへ接続した。SSH・ファイアウォール設定は変更していない。
- VPS専用の取得先 `/home/ubuntu/tsunagumo-release-stage-20260919` を作成し、Ubuntuの既存GitHub認証でclone。旧リリース用cloneは権限不足、rootのGitHub鍵は未設定だったため、所有者・鍵を変更せず独立cloneへ切り替えた。日次QA用cloneは使用していない。
- saas/websiteの283ファイルを双方のGitから取得。Windows/Linuxの改行差をLFへ正規化したファイル名・内容SHA-256一覧のハッシュは `667ccd691416d859a2c5ea0d6512618b65cffa9a1bbf9ac6bcc64a79529a0f02` で一致。
- VPSのtar自体のSHA-256は `827bdf184a23edd763e40f4256d309156a2e86a36bb3878dffbc028ef19c8b8e`。PC側tarのハッシュとは一致しないが、改行正規化後の各ファイル内容は一致した。
- `/root/tsunagumo-release-20260919`（0700）に更新前コード・ENVを含む `previous-code.tar.gz`、暗号化DB `db.dump.enc`（117,200 bytes）とロール `db-globals.sql.enc`（912 bytes）、配布物・manifest・検証記録を保存。
- DBは既存バックアップ用パスフレーズによるAES256CBC/PBKDF2で暗号化。pg_restore一覧取得と、暗号化→復号のバイト一致を確認。今回のバックアップを隔離DBへ復元するテストは未実施（前日のバックアップでは実施済み）。
- 切り戻し用イメージ：`tsunagumo-rollback-api:20260919` / `tsunagumo-rollback-scheduler:20260919`。本番Composeはプロジェクト名 `docker` と実ファイル2つ・変数展開用ENVを明示。
- `deploy.py` は更新失敗時に今回の更新前コード・ENV・イメージへ戻す処理を備える。今回の更新は成功し、切り戻しは未実行。DBボリュームの削除・ダウングレードはしない。

## 顧客別の未登録連携

現行本番でも、SaaSテナント用LINEチャネル0件・受信メールアカウント0件。接続確認済みの営業LINEとは用途が異なるため、そのWebhookを置き換えていない。利用するテナント・LINE窓口・受信メール認証の指定が必要。
