# 会社単位のプラン・社員共有（2026-09-25）

ユーザー指定：中間プランを追加。基本39,800円・中間59,800円は税込。同じ会社の全社員が利用し、やったことや成果物を共有する。実装後に、本番反映・Stripe設定・実費検証まで進める指示を受け、2026-09-25夜に反映した。既存契約は移行していない。最新の実施結果は次節、後半の準備記録は実施前の履歴。

## 本番反映の実施結果（2026-09-25 23時台 JST）

- 本番コードは `7cc3decba37c04a2b2c6c320e05e8d7807695864`。SaaS・社員共有画面・マイグレーション0025・公開4ページを反映。退避先は `/root/tsunagumo-company-plan-20260925_224105`。コード・イメージ・暗号化DBを退避した限定反映スクリプトが `DEPLOYED` で完了した。
- 実ENVのハッシュ不変、旧テナントのplan別件数の前後差分なし、basic=30/business=60を確認。API・scheduler・DBはrunning、再起動回数0。反映後APIの10分間とschedulerの25分間のログでERROR/Tracebackは0件。
- VPSから証明書検証を有効にしたHTTPSで公開料金ページ200、アプリの取得成功と「社員アカウントでログイン」「会社の共有履歴」の存在を確認。`/health` は存在せず404のため正常性判定に使わない。
- PCのChromeはアプリに `ERR_CERT_AUTHORITY_INVALID`、PCのHTTPクライアントにも証明書信頼エラーがあり、警告は回避していない。本番での社員ログイン・実招待メール・端末上の操作確認は未完了。開発環境の画面14件・権限分離等の自動テスト成功とは区別する。

### 続行時の証明書エラー調査

PCの接続先は大学のWi-Fiで、テザリングではなかった。DNSは本番IPを返したが、TLSで受け取る `CN=app.tunagumo.com` の証明書のIssuerはFortinetだった。Windowsの通常チェーン検証で `RemoteCertificateChainErrors` となり、接続を拒否した。証明書検証を無効化せず、信頼ルートの追加・ファイアウォール変更もしていない。通信経路のSSL検査による再署名と整合する（[Fortinet公式説明](https://docs.fortinet.com/document/fortigate/7.0.0/administration-guide/122078/deep-inspection)）。本番サーバー証明書の交換を原因対策として実施しない。

ユーザーへ自分のスマホのテザリングへの接続切り替えと、本人宛テスト招待メールの受取先を確認中。再開時は接続プロファイルと通常TLS検証の成功を確認し、Chromeで本番画面を開く。招待先・対象会社を特定してから本人宛に招待し、受信・初期設定・個人ログイン・共有履歴を順に検証する。既存顧客や社員を試験宛先にしない。招待作成はアカウント状態を変えるので、送信先不明のまま実行しない。

続行結果：ユーザーのテザリング切り替え後、PCの通常証明書検証付きHTTPSは200となり、Chromeでも警告なしで本番アプリを開けた。既存の会社キーによるログイン状態で「会社の共有履歴」の依頼状態・承認履歴、そこからの成果物表示、利用状況の会社共有枠・次回更新日時を確認した。対象会社の旧ライト契約は維持されている。ユーザー本人から招待先を受領し、会社「ツナグモ」の最初の管理者招待フォームを準備した。

管理権限作成の直前確認に対するユーザーの「やったよ」の後、本番画面に「招待メールを送りました。受信者が48時間以内にパスワードを設定してください。」と再送ボタンが表示されていることを確認。すでに送信済みなのでCodexからの重複送信は行わなかった。受信・パスワード設定・個人ログインの成功は未確認。次は本人が招待メールのリンクからパスワードを設定する。ブラウザー操作の安全ルールにより新しい認証情報の入力・登録は本人に引き継ぐ。パスワードや招待コードをチャット・Gitへ記録しない。

未着の申告後、本人の接続済みGmailで送信元・件名・直近2日を絞って検索したところ、招待メール3通（初回1通・再送2通）はすべて迷惑メールに分類されていた。最新の2026-09-25 23:30:08 JSTの1通だけSPAMを外してINBOXに移動し、UNREAD・INBOXかつSPAMなしを再検索で確認。送信障害ではなく迷惑メール分類による未発見だった。再送ごとに招待コードが更新されるため最新のメールを利用する。今回Codexからの再送はなし。本人のパスワード設定・個人ログインの検証は引き続き未完了。

### 招待画面が保存済みログインで隠れる不具合

「押したよ」の後にサーバーで対象管理者の状態だけを読み取り、password_set=false・active=false・invite_pending=true・ログイントークン件数0を確認。ブラウザー操作はrequest-header-policyの読込失敗で画面確認前に止まった。サイトの障害とは区別する。

ソース確認で、招待リンクから初期設定欄を開いても保存済み会社キーがあると自動ログインで画面全体が隠れることを発見。修正前の既存14画面テストは成功、新しい招待リンク回帰テストは非表示で失敗した。招待パラメーターがある場合は起動時の自動ログインを行わないよう修正後、15件すべて成功。保存済みキーを勝手に削除せず、招待コードのURLからの除去も維持する。テストは既存のChromium headless shell build1228とプロジェクト既存venvで実行した。

この表示修正はGit側のみで、本番にはまだ反映していない。SSHのubuntuは接続可能・docker操作可能だが本番frontendはroot所有で書込不可、sudoはパスワード必須、root SSHは認証拒否だった。既存のrootコンソール等の正規の経路が復旧したら、frontend/index.htmlだけをバックアップ・差分照合して反映し、稼働API内の配信HTMLと公開HTMLを照合する。現時点の操作回避策は、通常のアプリ画面ならログアウトし、最新の招待メールをもう一度開いて「新しいパスワード（8文字以上）」を入力して「設定する」を押す。パスワードは本人が設定する。

2026-09-26 00:01 JSTの追加調査：ユーザーの設定不可申告後、accept-inviteの400とmember-loginの401を確認。対象アカウントはpassword_set=false、active=false、invite_pending=true、失敗ログイン計数0だった。Gmailには9/25 23:56:13の新しい再送メールがUNREAD・INBOXで存在し、DBの招待有効期限もその送信時刻の48時間後と一致する。以前案内した23:30のコードは再送で無効となったため、最新23:56のメールを案内し、追加再送をしないよう伝えた。ブラウザー操作は引き続き接続ポリシーの読込失敗で画面を確認できていない。400の画面上の詳細文言は未確認。

### Stripe・LINE

| プラン | Stripe price | Payment Link |
|---|---|---|
| basic | `price_1UJZS7Gj5LbAzw5N48iWzNz7` | https://buy.stripe.com/9B66oHdq84EMfrc5eqfUQ02 |
| business | `price_1UJZTjGj5LbAzw5NoElecOWp` | https://buy.stripe.com/aFacN571Kc7e92O8qCfUQ01 |

Stripe本番で新しい単独商品と価格を作成。APIで有効・JPY・月1回・税込・39,800/59,800円を照合した。両リンクは保存後の管理画面で単独商品、数量1固定、14日トライアル、決済手段登録必須、税込、自動税計算、規約同意必須、有効を確認した。リンクIDはbasic=`plink_1UJZhQGj5LbAzw5NYqdoR39G`、business=`plink_1UJZdpGj5LbAzw5N8SePo5Cf`。制限付きキーはPayment Linksの読み取りも403で、リンク検証は画面で行った。

合計335,800円/月になっていた旧合算リンク `plink_1UGHhXGj5LbAzw5NtCCcOM5y` は新リンク検証後に無効化し、「無効」表示を確認。旧商品・価格・契約は変更していない。Stripeの公開規約URLを `https://tunagumo.com/terms`、プライバシーを `/privacy`、特商法を `/law` に設定した。実際の購入、顧客課金、メール送信は行っていない。

リンクは相談後の運営案内用。Payment Link経由では運営が顧客とSaaSテナント・planを対応付ける必要がある。公開サイトへ無条件の即時申込リンクは追加していない。LINE管理画面の開始処理は選択planに対応するSaaS会社枠も作成する。

Workerは上記2価格IDを設定し、`wrangler deploy --keep-vars` で反映。新version=`6eda50df-2902-4d4c-997f-c5dd897e975e`、直前の切り戻しversion=`5e7842f6-38ab-45d5-b7d8-52c4a8e50240`。既存vars・secretsを維持したことをversions viewで確認。VPSからWorkerの `/reserve` は200、未認証 `/admin` は401。PCのPythonクライアントはCloudflare 1010で403だったため、VPSから通常の証明書検証付きリクエストで確認した。予約送信・契約開始は未実施。

### 実APIによる文章業務の費用検証

本番コンテナの既存OpenAI設定（主モデルGPT-6 Sol）で架空の3業務を実行。全24回の応答usageを積算した概算原価は **0.1511345 USD**、usage欠落なし。提供元請求書の確定額ではない。3件ともQA=PASS、AWAITING_APPROVALで止まり、外部送信なし。

| 業務 | 呼び出し回数 | 秒 | usageによる概算USD |
|---|---:|---:|---:|
| 物件紹介文 | 12 | 99.93 | 0.082869 |
| 反響対応メール案 | 6 | 37.74 | 0.032421 |
| オーナー月次報告 | 6 | 39.26 | 0.0358445 |

生成文も確認した。紹介文は提示された賃料・間取りを保持し、徒歩算定・ペット追加費用等を要確認とした。返信案は内見の候補時刻を示し、入居可能日を未確認とした。月次報告は申込審査中を成約扱いせず、施策の効果を断定せず、フリーレントを承認前の提案として扱った。紹介文の汎用的な末尾表現にはQAの注意が残るため、公開前の人による確認は必要。

この3業務と同じ比率・コストなら30 USDは計算上約595件相当。ただし3件の架空文章だけによる参考換算で、全社員の月間十分性や件数を保証しない。画像・音声・資料取込・実データでの再生成・LINE応答・固定費等は含まない。実測原本はVPSの `/root/company-benchmark-20260925.json`、stderrは同名 `.err`（非推奨警告のみ）。

## 新規契約用の設定

| プラン識別子 | 表示名 | 月額税込／社 | 社内全員の共有枠（運営用USD） |
|---|---|---|---|
| basic | ベーシック | 39,800円 | 30 |
| business | ビジネス | 59,800円 | 60 |
| enterprise | エンタープライズ | 個別見積もり | 個別契約の正の有限値。未設定は生成不可 |

社員数による追加課金なし。基本と中間の機能は共通。社員を追加しても枠は増えない。別法人の共有利用は個別相談。旧 `light=10 / standard=40 / unlimited=120 USD` は維持し、既存契約を自動移行しない。管理APIの新規作成は未指定時basicになるため、新規発行時はStripe契約と対応するplanを明示する。

顧客に内部原価を返さず、会社全体の使用割合・残量・更新日時を表示。80%以上は画面に警告、使い切ったら新規生成を拒否。毎月1日00:00 UTC（日本時間09:00）に枠更新。作成済み資料の閲覧・ダウンロードは枠消尽だけでは止めない。新プランから旧追加チケットの決済作成は拒否。本人が相談してプランを変更するまで追加課金しない。

## 30 USDで足りるか

通常利用量の実データは未取得。上記の架空3業務の実測は完了したが、全社員で十分・月何件保証、とは判定していない。以下は実測前の計算例として残す。

例えばGPT-6 SolのStandard短文脈を使い、1件の業務全体（複数のAI呼び出しを合算）が入力30,000・出力8,000トークンなら、1件0.14 USD。100件で14 USD。画像を1枚0.25 USDと仮定して20枚なら5 USD。自動応答1回を入力3,000・出力400トークンと仮定して500回なら5 USD。合計24 USDとなり、30 USD以内には収まる。

これは**仮定を置いた計算例**であり、実測値・標準的な利用・上限の保証ではない。画像0.25 USDはコードのフォールバック見積もりで、品質・解像度・入力画像により変動する。長文・再生成・音声・資料取込の利用も加わる。契約前に想定業務を確認し、トライアル実績から適切な枠を選ぶ。営業ページにこの例の件数を保証として掲載しない。

単価の一次資料（2026-09-25確認）：
- [OpenAI料金](https://developers.openai.com/api/docs/pricing)：GPT-6 Sol Standard短文脈、入力2 USD・出力10 USD／百万トークン。
- [埋め込み](https://developers.openai.com/api/docs/models/text-embedding-3-small)：0.02 USD／百万トークン。
- [Whisper](https://developers.openai.com/api/docs/models/whisper-1)：0.006 USD／分。

## 原価管理の範囲と限界

会社ごとのPostgreSQLトランザクションロックで、複数社員・API・バックグラウンド生成の判定と計上を直列化。グラフ実行の上限は既存の1依頼上限と残量を考慮する。生成結果の増分計上はSessionとTenantの行ロックで更新の取りこぼしを防ぐ。

文章、画像、問い合わせ返信案、Web・LINE・メール自動応答、資料の埋め込み、字幕起こしを集計。ルームツアーは既存の1回0.15 USDという概算を継続しており、実費照合が必要。Higgsfieldによる映像の新規生成は顧客自身の契約・負担で、運営枠と別。

提供元の請求を正確に30 USDで遮断するハードリミットではない。実行中の一回が残量を超えることがあり、新プランではその超過原価も切り捨てず記録して次の生成を停止する。失敗したAPI呼び出し・返却usage不足・単価改定の請求差異は提供元明細との照合対象。原価上限と利益を保証しない。

## 社員ログインと共有

- 管理者が社員をメールで招待。招待受諾は48時間以内、個人ログインは会社ID・メール・パスワード。社員への会社APIキー配布は不要。
- 会社キーで最初の管理者を招待できる。以後の社員招待・会社設定・契約操作は管理者。一般社員は依頼・閲覧、承認者は承認も可能。
- 個人トークンは12時間。ログアウトは当人の全端末トークン失効。社員利用停止も全トークンを失効させ、最後の有効管理者は停止不可。
- ログイン失敗5回で当該個人アカウントを15分ロック。招待受諾・初回管理者作成・重複招待はDBロックで競合対策。
- 「会社の共有履歴」で、依頼者・依頼内容・状態・成果物へのリンクを閲覧。承認・差し戻し・画像作成・引き継ぎメモも共有。各一覧は最近100件。
- 一般社員も会社の成果物を閲覧できる。個人用の非公開依頼・部署ごとの制限は本変更の対象外。別テナントの成果物・共有メモは404で拒否。
- 旧履歴や定期実行には個人の依頼者情報がないので、その旨を表示。過去の作成者を推測して埋めない。

## 公開前の反映手順

1. 対象はsaasのコード・frontend、マイグレーション0025、websiteの料金・FAQ・トップ・特商法、line-botの説明。新プランだけでなく社員共有も同じ版で反映する。
2. `SYSTEM_PROMPT.md` §6 に従い本番反映の明示指示後、現在の配布物とDBをバックアップし、旧版tarの復旧先を記録。
3. 本番のENV供給元は `saas/.env`。マイグレーション0025を適用してAPI・schedulerを更新。開発テストでは必ず `-p tunagumo-dev --env-file ../.env.test`。
4. 既存契約は維持。Stripeには税込39,800円／59,800円の単独商品・月額・14日トライアルの申込導線を用意し、価格とplanを照合。既存の複数プラン合算リンクは新導線検証後に停止する。顧客に決済テストや実メールを無断で送らない。
5. 現在のStripe連携は有効・無効の同期が中心で、契約プランの自動変更は未実装。管理APIによる枠変更だけでは請求額は変わらない。運営が合意済みの請求変更と枠を一致させる。
6. 本番確認：既存テナントの旧枠、新規basic/business、管理者と社員のログイン、会社間分離、招待メール、共有メモ、80%表示、枠消尽時の既存資料閲覧、税込料金。
7. 切り戻しは旧アプリ・静的ページに戻す。0025は追加列のみなので旧コードでも動作する。社員が利用を始めた後にDBを過去のバックアップで無条件に上書きしない。

## 検証結果

- 変更前のバックエンド667件、実装後の全体675件が成功。月替わりの回帰テストを追加し、保存時の日時形式と別接続間のロック待ちを修正後、関連93件が成功（会社プラン・社員権限・共有履歴・原価管理・グラフ実行・問い合わせ・提案）。
- Playwrightの画面テスト14件が成功。社員ログイン、利用量80%の表示、追加購入ボタン非表示、共有メモ・成果物リンク・HTML無害化を含む。
- LINE botの既存テスト、Python構文検査、`git diff --check`が成功。DBマイグレーション0025は開発用DBに適用済み。
- 開発テスト時点では代替プロバイダー・架空の利用者で検証。その後の本番反映・Stripe・実API結果は冒頭に記録。実際の招待メール送信・負荷試験は未実施。

反映後は「社員・会社情報」から最初の管理者と社員を招待し、各自でログインする。「会社の共有履歴」で依頼・進捗・成果物を確認し、「引き継ぎメモを共有する」から社内コメントを残せる。

## 続行時の確認と追加実装（反映前の準備記録）

- LINEの新規予約はbasicを記録。管理画面で顧客と合意済みのbasic/businessを選び、同じプランでStripe契約とSaaS会社枠を作る。旧予約は従来プランを維持する。
- 新プランはStripeの価格が有効・JPY・税込・月1回・39,800/59,800円と一致することを課金開始前に照合する。不一致・未知のプラン・開始済み予約は停止。Stripe作成には予約単位の冪等キーを渡す。
- `STRIPE_PRICE_BASIC` / `STRIPE_PRICE_BUSINESS` は未設定。Stripeで新価格を作成して実データを読み直した後に `line-bot/wrangler.toml` に設定し、Workerを反映する。それまでは新プランを開始しない。Stripeの制限付きキーによる商品・Payment Link作成は403で拒否され、既存キーの権限拡張は行っていない。
- LINE新規テスト7件、既存3経路テスト、費用検証ガード5件が成功。費用検証のpreviewはAPI呼び出し0件を確認した。

### 本番反映の準備

`scripts/deploy_company_plan_20260925.sh <確認済み40桁commit>` を用意し、Bash構文検査済み。未実行。VPSの本番外release cloneで作業ツリーがクリーンか確認して、対象commitを取得する。rootでこのスクリプトを起動する前に、対象サーバー・稼働コンテナ・ディスク空きを再確認する。

スクリプトは旧コード・イメージ・暗号化DBバックアップを確保してから限定ファイルを配置し、0025を適用してAPIとschedulerだけ更新する。実ENVのハッシュ不変、画面要素、会社枠、DB版、公開4ページ一致と3コンテナ稼働を検証する。失敗時は旧コード・イメージへ戻す。バックアップ先は実行時の `/root/tsunagumo-company-plan-日時`。旧契約のplan別件数を前後のファイルに保存する。社員ログイン等の実画面検証は別途必要。

2026-09-25の接続確認ではSSH IPv4はタイムアウト、IPv6も接続不可。Chromeのシリアルコンソールはrootプロンプトまで確認したが、その後操作接続が切れ、管理画面がログイン画面へ戻った。本番への書き込みはしていない。Stripeも操作接続が不安定で商品作成を完了していない。再ログイン・拡張機能の接続復旧後に続ける。

### 実費検証の準備

`saas/scripts/benchmark_company_plan.py` は架空の3業務を実際の統括・部署・品質確認グラフで実行する。DB保存、資料検索、外部送信、承認の再開はしない。OpenAI応答のusageを検証用単価表で積算し、再生成も計上する。保守的なリクエスト見積もり合計1 USDで次の呼び出しを停止し、usage不明・通信結果不明でも以後を止める。これは提供元請求の厳密なハードリミットではない。

既存の有効なAPIキーがある環境で、saasを作業ディレクトリとして `python -m scripts.benchmark_company_plan --run` を明示実行する。指定なしは課金しないpreview。VPSの既存コンテナで試す場合は、確認済みrelease cloneのスクリプトを標準入力で `docker exec -i -w /app docker-api-1 python - --run` に渡せる（既存のcompare_writing_models.pyがあることを先に確認）。キーをPCへコピーしたり、ログへ出す必要はない。

ローカルの既存APIコンテナにもOpenAIキーがないことを確認したため、有料生成は未実行。実行後は状態・成果物の品質・呼び出し数・USDを記録する。未完了の業務を成功1件の単価に含めない。画像・音声・資料取込は今回の文章グラフ検証に含まず、3件だけで全社員の月間十分性を保証しない。

## 管理者の初期設定・個人ログイン完了（2026-09-26）

ユーザーの設定完了報告後、対象ユーザーの状態をサーバーで読み取り、active=true、password_set=true、invite_pending=false、role=owner、有効期限内のログインセッション1件を確認した。パスワード設定と個人アカウントでのログインが完了している。パスワード・トークン値は取得・記録していない。先に追加した招待画面優先表示の修正（df9af54）はGit側のみであり、本番反映待ちという記録は維持する。

## 順次対応1：招待画面修正の本番反映完了（2026-09-26 00:31 JST）

ユーザーの「一から順にやって」に基づき、df9af54のfrontend/index.htmlを反映。上記の本番反映待ちは解消した。通常のubuntu SSHと既存Docker管理権限を使用した。ホスト側のroot書込はネットワークなし・capabilityなし・新規権限取得禁止・読み取り専用ルートFSの一回限りコンテナで行い、書込マウントは本番frontendディレクトリだけに限定。Docker socket・ホスト全体・秘密ENVをマウントしていない。SSHやsudoの認証設定は変更していない。

- 反映手順：`scripts/deploy_invite_ui_20260926.sh`（Bash構文検査済み）。稼働イメージIDと反映前HTMLハッシュの一致を条件に、元のHTML・イメージを退避して原子的に置換。稼働コンテナ内の静的HTMLとホストのビルド元HTMLを更新し、同じHTMLを含む再作成用APIイメージも既存イメージからネットワークなしで作成してdocker-api:latestへ設定した。
- 公開・ホスト・稼働API内・再作成用イメージのSHA-256はすべて `dfc58e662cba38094d47349c867210d7b95c3dce1f30f64deed9646bfef61fca`。PCからも通常TLS検証付きで公開HTMLの一致を確認。API・scheduler・DBのコンテナID、稼働状態、再起動回数は反映前後で同じ。DB・ENV変更なし。
- 退避：`/home/ubuntu/deploy-invite-ui-20260926/backup-20260926_003113`。旧イメージ：`tsunagumo-invite-rollback:20260926_003113`。新イメージ：`tsunagumo-invite-ui:20260926_003113`。問題があれば退避index.htmlを同じ原子的置換手順でホスト・稼働APIへ戻し、旧イメージをdocker-api:latestへ戻す。稼働コンテナのimage ID自体は再起動していないため旧IDのままだが、配信ファイルと再作成用タグは更新済み。
- 画面テストは当該HTMLについて修正前14件、修正後15件成功済み。実ブラウザー連携は別途request-header-policy読込エラーが残り、本番の実画面を再操作したとは扱わない。

## 順次対応2：顧客用LINE・受信メールの接続準備

会社「ツナグモ」の本番設定を値を出さずに確認：LINEのsecret/tokenはいずれも未設定、受信メールアカウント0件、通知先メール設定あり、緊急連絡電話なし。本人に試験用LINE公式アカウント名と受信メールアドレスを確認中。先ほどの管理者招待メール送信の成功は、受信メール監視・自動返信の接続完了を意味しない。

試験窓口が確定したら、既存営業LINEのWebhookを転用せず、対象窓口の認証情報を管理者本人がアプリへ登録する。接続後に本人が送った通常問い合わせへの一次回答、担当者への引継ぎ、架空の緊急問い合わせへの安全案内と通知、アプリの問い合わせ履歴を順に確認する。実顧客への試験返信は行わない。現段階では送信・新規アカウント発行・設定変更なし。
