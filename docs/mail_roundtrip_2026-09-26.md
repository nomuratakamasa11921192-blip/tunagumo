# メール自動返信の実送受信確認（2026-09-26、Codex）

## 試験範囲

本人の「やって」という指示を受け、接続済み本人Gmailへのメール1通から受信・定期処理・自動返信・返信の実受信まで確認した。
別の本人メールアドレスはないとの回答だったため、既存ResendアカウントのReceiving画面に表示された `eiwhapue.resend.app` を返信確認先に使用した。
新規アカウント、DNS、Webhook、秘密鍵、メールボックスの全件処理への変更は行っていない。

- 送信元: 既存の `notifications@tunagumo.com`
- 宛先: 本人指定の `nomuratakamasa11921192@gmail.com`
- Reply-To: `mail-test-20260926@eiwhapue.resend.app`（同じ本人Resendアカウントの受信アドレス）
- 件名の先頭: `[TSUNAGUMO-TEST]`。個人メールの対象制限を保持。
- 内容: 運営者の動作確認であり、実際の物件相談・予約依頼ではないと明記。
- 各送信に固定のIdempotency-Keyを設定し、二重送信を防止。外部の顧客宛てには送信していない。

## 1通目の結果

- 件名: `[TSUNAGUMO-TEST] メール自動返信の動作確認 20260926-01`
- Resend送信ID: `01a0ddc5-969c-7291-af43-eea818b0298f`、配達状態 `delivered`。
- 自動返信の受信ID: `6a89244c-59cb-4f77-b424-ddceff38fb4c`、受信時刻 `2026-09-26T12:52:39.769Z`。
- 実受信したFromは本人Gmail、Toは上記試験用受信先、件名は元件名に `Re:` が付いたもの。`Auto-Submitted: auto-replied` も確認。
- DBの処理済み記録で `auto_replied=true`、メール接続の `last_error` なし。
- 本文は「担当者から折り返しご連絡いたします。少々お待ちください。」。公開用資料がないため、推測回答せず担当者へ回した。
- 問い合わせID `8400e545-319d-4ab5-9905-271c80ed3fd5` を実画面で確認し、試験記録を対応済みに変更。手動返信は送信していない。

## 発見した不具合と修正

返信署名に登録会社名ではなく、業種テンプレートの「サンプル不動産株式会社」が出ていた。
`src/agent/mail_scan.py` のAI応答への会社名と署名の会社名を、共通テンプレートからテナントの登録名 `tenant.name` に修正した。

- 修正前のメールテスト34件成功。
- 登録名2種類について、実際の署名とAIへ渡す会社名を検証する回帰テストを追加し、修正前の2件失敗を確認。
- 修正後、メール・問い合わせ関連66件成功。
- コード: `f76393d`。反映スクリプト: `scripts/deploy_mail_name_20260926.py`。
- 本番の変更対象は `src/agent/mail_scan.py` 1ファイル。ホスト・稼働API・scheduler・再作成用2イメージでSHA-256一致を確認。
- 反映後SHA-256: `577ff39bea33f19cf8dd8ac25bc6c75c1ef37474cdd06ac8f6771b3171152055`。
- バックアップ: `/home/ubuntu/deploy-mail-name-20260926/backup-20260926_220814`。
- 切り戻し: `tsunagumo-mail-name-rollback-api:20260926_220814` / `tsunagumo-mail-name-rollback-scheduler:20260926_220814`。
- 新イメージ: `tsunagumo-mail-name-api:20260926_220814` / `tsunagumo-mail-name-scheduler:20260926_220814`（各latestタグへ設定済み）。
- API・schedulerを再起動して公開応答成功を確認。コンテナID・ENVを保持。DBは再起動・変更していない。

## 修正後の再確認

修正結果を確かめるため、同じ本人宛て・返信確認先へ追加の1通だけ送信した。
件名は `[TSUNAGUMO-TEST] メール自動返信の修正確認 20260926-02`。
Resend送信IDは `01a0ddd5-afa1-75b0-9581-9c18c91f7c3e`、配達状態 `delivered`。
自動返信の受信IDは `aeb3e01c-76f4-4765-a3d9-ca358a4068f6`、受信時刻は `2026-09-26T13:10:53.512Z`。

実受信した本文で「このメールはツナグモの自動応答です。」と表示され、サンプル名がないことを確認した。
Fromは本人Gmail、Toはテスト受信先、件名は02の元件名に `Re:` が付いたもの。`Auto-Submitted: auto-replied` も確認。
DB上のテスト受信先に対する処理件数・自動返信件数はともに2件で、接続エラーなし。
2通目も公開用資料がないため担当者への引き継ぎとなり、UI上でテストの問い合わせを対応済みに変更した。
実メールの往復確認と署名の修正は完了。返信対象の件名限定を維持している。

## 検証の範囲

これは実メールの受信・自動返信・宛先への到着を確認する試験。公開用資料に基づく具体的な物件回答の品質や、全メールサービス・全障害パターンを検証したものではない。
