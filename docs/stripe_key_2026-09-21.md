# Stripe本番キーの反映（2026-09-21、Claude）

Codexが中断した「制限付きAPIキーの発行と本番反映」を完了した。前日までの記録は
[release_2026-09-19.md](release_2026-09-19.md)。

## 結果

本番からStripeへの疎通 **200**。4つの権限すべてを実APIで確認済み（テスト用オブジェクトは未作成）。

| 権限 | 確認方法 | 結果 |
|---|---|---|
| Prices 読み取り | GET /v1/prices | 200 |
| Customers 読み取り | GET /v1/customers | 200 |
| Checkout Sessions 書き込み | POST（パラメータ無し） | 400 parameter_missing ＝権限あり |
| Customer Portal 書き込み | POST（パラメータ無し） | 400 parameter_missing ＝権限あり |

書き込み権限は、わざとパラメータを欠いたリクエストで判定した（403＝権限なし、400＝権限あり）。
実際のCheckoutセッション・ポータルセッションは作成していない。

反映後: api・scheduler を再作成、db は7日連続稼働を維持（再作成なし）。
`https://app.tunagumo.com` と `https://tunagumo.com` が200、再起動後のERROR 0件。

## 記録の訂正

**Codexの「本人確認未完了のため未発行」は誤り。** キーは09/20時点で発行済みだった。
再開時はまず一覧で発行済みかを確認すること。

権限4種は発行時点で正しく設定されており、変更は不要だった。

## つまずいた点（次回のため）

1. **シークレットは作成時に一度しか表示されない。** 表示中に画面を切り替えると失われる。
   2回ローテーションをやり直した。表示されたら、保存が完了するまで画面を触らないこと。
2. **受け渡しはクリップボード経由が確実。** Stripeのコピーボタン → PowerShellの
   `Get-Clipboard` でファイルへ直接書き込む。手で貼り付けると、履歴から古い値を
   呼び出す事故が起きる（実際に3回起きた）。値を画面にも記録にも出さずに済む。
3. **コンテナに渡るENVは `/opt/tsunagumo/saas/.env`。** Composeの `env_file: ../.env` を
   見ること。`saas/docker/.env` はCompose内の変数展開用の別ファイルで、
   ここに書いてもコンテナには渡らない。最初これを取り違えて反映が空振りした。
4. 反映前に入っていた旧 `STRIPE_SECRET_KEY` は、そもそもStripeキーの形式ではない値だった。
   Codexが観測していた401の原因はこれ。

## 残っているもの

- 失効待ちの制限付きキーが2本。いずれも値は不明で未使用、7日で自動失効するため対応不要。
- 現行キーの値は、Git対象外の `saas/workspace/vps-import-20260918/private/stripe-production.env`
  にある（本番VPSの `.env` にも反映済み）。VPS上の一時ファイルは削除済み。
- 切り戻し用に `/opt/tsunagumo/saas/.env.bak-<日時>` を作成済み。
