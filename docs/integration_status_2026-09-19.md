# 連携の継続確認（2026-09-19、Codex）

前日の記録は [integration_status_2026-09-18.md](integration_status_2026-09-18.md)。VPSの1,994ファイルのPC回収は完了済みで、再転送は不要。

## 今回完了した設定・確認

- Stripeへのログイン成功を確認。対象は既存3プランを持つ `acct_1TsIT4Gj5LbAzw5N`。ライト商品・月額39,800円・既存価格IDの一致を画面で確認した。
- 既存の本番Webhook `we_1UAe31Gj5LbAzw5Nxnh1S4HX` に `checkout.session.completed` を追加して保存し、配信対象3イベントを確認した。既存の `customer.subscription.updated` / `customer.subscription.deleted`、URL `https://app.tunagumo.com/webhooks/stripe`、署名キーは維持した。Stripe側の登録完了であり、アプリへの実配信成功は未確認。
- 変更前に開発専用ComposeでWebhookテスト13件成功（4.94秒、警告32件）。購入完了の追加AI予算計上・重複イベント処理を含む。実際の課金・購入は行っていない。
- 既存カスタマーポータル `bpc_1UBaGVGj5LbAzw5NSpjFbZYi` は有効。契約期間終了時の解約と支払い方法変更が許可されていることを確認。設定変更は不要だった。
- Higgsfieldは、回収済み暗号化DBから本人テナントの既存APIキーだけをメモリー内で復号し、架空のリクエストIDの状態取得を実行した。HTTP404で、認証拒否の401ではなかった。生成・クレジット消費は行っておらず、モデル利用可否・API残高・実生成の成功は未確認。

## Stripeキーの準備

回収した配布物のENV36ファイルを照合したが、対象アカウントの有効キーは回収できなかった。唯一の候補はHTTP401。PC既存キーは別アカウントのため流用しない。

ダッシュボードで既存キーは再表示できず、新しい制限付きキーの作成フォームを準備した。発行はまだ実行していない。

| 項目 | 入力済みの内容 |
|---|---|
| 名前 | `tsunagumo-saas-production-20260919` |
| Checkout Sessions | 書き込み |
| Customer Portal | 書き込み |
| Customers / Prices | 読み取り |
| その他 | なし |

アプリの実装が使用するCheckout・ポータル作成と、既存顧客・価格の照合に合わせた初期権限。発行後は実APIで必要権限を検証し、根拠なく全権限へ拡大しない。既存キーのローテーション・失効は行っていない。

キーの保存先として、Git対象外の `saas/workspace/vps-import-20260918/private/stripe-production.env` に空の `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` を用意した。アクセス権はPC本人とSYSTEMのみ。秘密値をチャット・Git・ログへ出さない。

## 配布物の準備

- コード確定点：`04de792947024abc24a5217ced26bf954e86b02c`。前回反映 `0dacb8f` との差分は、Higgsfieldクライアントとその回帰テストの2ファイルのみ。
- `saas/workspace/vps-import-20260918/release-04de792.tar.gz` をGit追跡対象から作成。saas/websiteの283ファイルを収録。
- SHA-256：`fe8b0ea1ce8a162e2a8156eaceed0803f1d5801898078436e77850278580f005`。
- 絶対パス・親ディレクトリ参照・リンクがないこと、実ENV・private・workspace・Git管理情報を含まないことを検査。ENVのサンプル・テンプレートは収録する。ファイル別SHA-256一覧と検査記録を同じGit対象外フォルダに保存。
- 本番への転送・展開は未実施。
- PCの開発専用Composeで現在のsrc/testsを読み取り専用マウントし、全体テスト **662件成功、失敗・スキップ0件**（110.59秒、警告1,855件）。開発用プロジェクト名とテストENVを明示し、本番コンテナは使用していない。

## 残る作業と再開条件

1. 制限付きキーの発行直前確認をユーザーへ提示済み。確認後に発行・保護保存・対象アカウント照合を進める。
2. 既存Webhookの署名キーをアプリ用に保護保存する。回収した更新前ENVでは未設定だったため、現行本番を再確認してから適用する。署名キーを新規作成・変更する必要はない。
3. PCからVPSへのSSHはタイムアウト。さくら管理画面もログイン切れのため、ユーザーへログインを依頼済み。本番への接続・設定適用は今回未実施。
4. 指定通知先は本人テナントの問い合わせ設定に保存済み。共通の管理者通知先ENV、通知送信元、本人宛ての実送信確認は未完了。既存の承認待ち通知があるため、送信元有効化時の送信対象も確認する。
5. Higgsfield修正 `d72fa41` はPC・GitHubに保存済み、本番には未反映。実生成検証は別途必要。Web版Plusの契約とSaaSからのAPI課金を混同しない。
6. SaaS側のLINE・受信メールは前回確認時に登録0件。営業LINEは接続確認済みであり、そのWebhookをSaaS用に付け替えない。導入するテナント・窓口の確定が必要。

今回のUI設定はStripeの既存Webhookへのイベント追加のみ。新規商品・契約・購入・顧客宛てメッセージ送信、本番ファイルの変更は行っていない。
