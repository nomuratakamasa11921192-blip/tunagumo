# VPS作業履歴とPCへの引き継ぎ確認（2026-09-18、Codex）

## 確認範囲と結論

GitHubを再取得し、`origin/main` の全66コミットの履歴、`CHANGES.log`、引き継ぎ資料、直近の変更ファイルを照合した。最新は9月18日04:06の `6fd1a67`。前回PCに取り込んだ `5f9a35c` との差分は日次QA記録7行のみで、コード変更はない。PCの `257925c` の修正を保持し、最新mainの記録も手動作業ブランチへ統合した。

**OpenAIキーはVPS本番へ設定した記録がある。** 9月15日の `893410a` に本番 `.env` への追加、9月16日の `cda108d` にキー差し替え後のコンテナ再作成・復旧が記録されている。「PCの設定にない」と「VPSにもない」は別であり、キーの新規発行が必要とは判断しない。

今回はVPSのSSH（既存エイリアス `tsunagumo-vps`、22番）が複数回タイムアウトした。WindowsとDockerの双方でSSH接続できず、同じホストの443番およびGitHubの443番にはTCP接続できた。原因は未特定であり、VPS停止や認証失敗とは断定しない。以下のVPS実施結果はGitに保存された当時の記録で、現在のENV・DB・未コミット変更・cron・会話ログを直接再確認した結果ではない。本番 `/opt/tsunagumo` は参照も変更もしていない。

## 作業の照合結果

| 時期・代表コミット | VPS側の記録・実装 | 引き継ぎ判断 |
|---|---|---|
| 9/7〜14 `bf916bf`・`1081bae`・`468aabe` | 二刀流の役割分担、日次QA、Instagram確認、CLI起動設定を整備。旧日次QAはDocker権限・GitHub認証不足でテスト未実行／push失敗が続いていた。 | 旧「修復完了」というコミット名だけでテスト成功とは判断しない。 |
| 9/15 `ff4a40b`〜`b270f4c` | 本番と開発のCompose名衝突を修正。開発専用DB・ENV、権限・再ビルド・migration・workspaceを整備。GitHub SSH pushを復旧、448件成功。 | 開発では `-p tunagumo-dev` とテスト用構成を維持。 |
| 9/15 `f12a203`・`0db887e` | 文章と画像をOpenAIへ移行。画像費用を運営負担にし、Higgsfieldを動画専用・顧客キー方式に分離。画像配信と予算控除、PDFへの画像掲載も修正。 | 実装済み。画像の実用品質・実費確認は別の未完了項目。 |
| 9/15 `253ec65`・9/17 `30631dd` | 不動産情報ライブラリの項目・四半期・人口タイル取得、市区町村選択を修正。実APIで市区町村74件、取引814件、人口メッシュ897件と要約を確認。 | 実API確認済み。再発行しないユーザー方針を保持。 |
| 9/15 `d644d61`・`2923e00`・`e1d15e7` | 外部URLのSSRF対策、サイズ上限、APIのlocalhost限定、セキュリティヘッダー、本番API仕様書非公開。 | コード取り込み済み。現在の本番設定は再確認していない。 |
| 9/15 `738e49c`・`81f8bc9` | Stripe通知の二重処理防止、追加予算の誤加算修正、定期実行の契約・予算確認と登録数制限、CSV数式対策。 | 実装済み。Stripeの実契約・解約設定との照合は残る。 |
| 9/15 `893410a` | 人間が本番へデプロイし、OpenAI設定追加、DBバックアップ、migration 0021、Caddy更新を実施。外部読み取り確認を記録。 | 過去の本番反映記録。今回の本番操作ではない。 |
| 9/15 `2a52222` | Instagram見回りを8・12・18時、承認済み投稿の予約を19時へ変更。Codex予備モデルも更新。 | 現在のcron稼働・直近投稿の成否はSSH復旧後の確認対象。 |
| 9/16 `147ae79`・`55e6e7a`・`5e39589` | 問い合わせ一次対応・緊急通知、テナントごとのLINE、IMAP/SMTP即レス、物件提案・追客・同意・配信停止を実装。migration 0022〜0024。 | 機能は実装済み。SaaSテナントでの実送受信完了とは扱わない。 |
| 9/16 `5e39589` の作業記録 | 人間による新機能の本番反映、0022〜0024適用、公開ルート・認証・メニューの外部確認。 | 当時の実施記録を保持。以後の全コミットの本番反映は未確認。 |
| 9/16 `87ea3a6`・`a3b73f2` | 全10画面のブラウザ確認、ポータル反響の取込、一覧・入力・LINEイベント数上限、営業サイトと設定手順を更新。 | コード取り込み済み。実際のポータル受信例との照合は残る。 |
| 9/16 `2b8c9d8`・`77db5ee` | ログインの同期処理によるサーバー停止を修正、画像180日削除、多段階承認の同一人物重複禁止、社内資料と社外検索の分離を検証。 | コード取り込み済み。保持期間ジョブの現稼働は再確認していない。 |
| 9/16 `5ea0337`・`6dced18`・`6c2509a` | 営業LINEボットをOpenAIへ移行、3プランの申込と価格IDを設定、実機でのツール呼出エラーを修正。 | 営業LINEボットとSaaS利用者のLINE連携は別。既存LINE設定を勝手に転用しない。 |
| 9/16〜17 `573741b`・`61928da`・`42d52a7` | 文章生成をAnthropic/OpenAIから選べる構成へ変更。AnthropicヘルスチェックとHaikuモデルIDを修正。 | 当初の「OpenAI一本化」計画は最新仕様ではない。文章のClaude対応を保持。 |
| 9/17 `ac6bfbc`・`73b908a` | 非同期応答による画面上書きを修正。顧客利用量を割合で表示し、追加枠APIからドル原価を除外。 | PCに取り込み済み。画面テスト11件もPCで成功済み。 |
| 9/17 `ca46d01`・9/18 `5f9a35c` | 日次QAの失敗停止、DB更新、監査後再テスト、保存・push照合、mainかつ変更なしという開始条件を追加。 | 回帰テスト24件成功済み。稼働中のVPSジョブは今回変更していない。 |
| 9/17 `c381336`・`a0eea4a` | ポータル連絡先抽出、提供元・動画費用の公開文面、屋内外別のガス案内を修正。 | 全646件成功の記録あり。法務・実契約の最終確認は残る。 |
| 9/18 `6fd1a67` | VPS開発Dockerで646件成功、失敗・スキップ0件、93.93秒。監査・保存記録のみ追加。 | 今回取り込んだ最新記録。PC追加分5件を含む651件とは対象コミットが異なる。 |
| 9/18 PC `257925c` | Windows環境とENVのイメージ除外を整備。原文条件の欠落を修正し、全651件・画面11件・QA24件・営業LINE3経路成功。Anthropic実生成と内部コスト一致も確認。 | PCブランチで保持。VPSのmainや本番へ自動反映していない。 |

## 設定の保存先と確認の限界

| 設定 | 確認できた保存先・過去記録 | 今回の状態 |
|---|---|---|
| OpenAI | VPS本番のSaaS `.env` へ追加・更新した記録あり。 | PCの確認済みENVにはない。VPS開発用に別途保存されたかはSSH接続待ち。本番からの取得はしていない。 |
| Anthropic | VPS本番で提供元を切り替えた記録あり。PCの `saas/.env` に設定あり。 | PCでは実API成功を確認済み。 |
| 不動産情報ライブラリ | VPS開発用 `saas/.env.reinfolib.local` に権限0600で保存した記録あり。 | PCには未転送。Gitでは同期されない。値の再表示・キー再発行はしない。 |
| LINE | PCルート `.env` にチャネル設定あり。営業ボットの実機修正記録あり。 | SaaSのテナント用設定と同一かは未確認。VPS開発DBは9/17時点で連携0件という記録。 |
| 通知メール | PCの `saas/.env` にResend設定あり。今回ドメイン一覧APIはHTTP 200。 | 送信元ドメインの検証状態は未確定。通知メールを送った結果ではない。 |
| IMAP/SMTP | SaaSの画面からテナントのDBへ暗号化保存する設計。VPS開発DBは9/17時点でメールアカウント0件という記録。 | ENVがなくてもDBに登録されている可能性はある。現在のVPS開発DBは未確認。 |

## 残る確認

1. SSH接続を復旧し、VPS開発cloneのブランチ・未コミット変更・開発用ENVの設定有無を値非表示で確認する。開発DBの連携件数、日次QA・cronの実行結果も開発対象に限って確認する。
2. OpenAIの開発用設定が利用できる状態で画像品質・コストを検証する。本番のファイルを参照しない現在のルールは維持する。
3. 対象テナント・宛先が確定したテストでLINE・メールの実送受信を確認する。今回は送信もWebhook変更もしていない。
4. 未push・未保存の会話／コマンド、本番の現在設定まで確認したと扱わない。実装済み機能を未着手として作り直さない。

今回の変更は記録と文書のみ。アプリコードの変更はなく、既存の成功結果を新たなテスト実行結果として数え直していない。差分・競合解消・main履歴の取り込みを検証した。
