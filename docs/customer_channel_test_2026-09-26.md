# 本人限定のLINE・個人Gmailテスト

2026-09-26 Codex。ユーザーの「おすすめでやって」により、既存営業LINEを維持する本人限定ルートと専用件名メール方式を採用した。

## 実装内容（本番反映済み・実接続待ち）

- メールの新規接続は件名先頭 `[TSUNAGUMO-TEST]` を既定値にする。管理者が空欄を明示したときだけ全件対象。画面には全件化の確認を表示する。
- IMAPで件名検索し、一致候補の件名ヘッダーを確認してから本文を取得。途中に文字列があるだけのメールは除外。検索エラー時は全件取得に切り替えない。受信処理側でも記録・AI・通知・返信の前に条件を再確認する。
- 接続先・件名条件・有効化状態を変更すると受信位置を初期化し、次回確認時点以前のメールへの返信を避ける。既存アカウントはmigration 0026で空文字を付与し、従来動作を維持する。
- LINE Workerは既存署名検証を通した後、設定した本人userId・1対1トーク・テキスト・先頭 `[TSUNAGUMO-TEST]` の全条件一致だけをSaaSへ転送。テスト印を除去し、抽出した1イベントをチャネルシークレットで再署名する。他の同梱イベントは転送しない。通常相談は既存営業処理を継続する。
- 本人テストの転送失敗は営業回答へフォールバックしない。テストルートの設定未入力時は従来動作のまま。二重回答防止は既存SaaSのwebhookEventId記録を使う。

## 実接続の前提と手順

現時点で実送受信は未実施。個人メール本文の閲覧、メール送信、LINEの実送信、Webhook URL変更は行っていない。
追加の「やって」で本番反映が明示承認され、2026-09-26 20:29 JSTに反映した。実際の認証情報登録・本人限定転送の有効化・試験送受信はまだ行っていない。

1. 本番反映が明示されたら、DB・現行API/schedulerイメージとソースを退避し、0026適用後にAPIとschedulerの両方を同じ版へ更新する。ENVは既存 `saas/.env` を維持する。APIだけを更新して旧schedulerでメールを有効にすると制限が効かないため、両方のコード照合が必須。
2. メール設定画面の「自動返信する件名の先頭」が表示され、`[TSUNAGUMO-TEST]` と確認してから設定する。受信 `imap.gmail.com:993`、送信 `smtp.gmail.com:465`、ユーザー名は本人指定Gmail。Googleアプリパスワードは本人が発行して画面に直接入力。チャットやGitへは貼らない。本人のGoogle認証が必要で、既存Gmailコネクターの認証を流用しない。
3. メールの最初の確認完了後、別の本人管理の送信元から専用件名の架空問い合わせを送る。同一アドレスから自分宛てのメールは既存ループ防止で対象外。送信元も本人の管理下であることを確認してから送信する。通常件名の私用メールが記録・返信されないことを確認する。
4. 本人のLINE userIdをLINE Developers画面などの本人情報で確定する。表示名や友だち一覧から推測しない。ユーザーIDを第三者宛てに設定しない。
5. 対象会社のSaaSに既存のLINEチャネル認証情報を安全な経路で登録する。外部出力・チャット・Gitには保存しない。SaaSのテナントIDと本人userIdをWorkerの `SAAS_TEST_TENANT_ID` / `SAAS_TEST_LINE_USER_ID` として設定する。既存secretを保持してWorkerを反映し、Webhook URLは既存営業Workerのまま維持する。
6. 本人が公式LINEの1対1トークへ `[TSUNAGUMO-TEST] 内見について教えてください` などを送る。通常問い合わせ、引継ぎ対象、架空の緊急問い合わせをそれぞれ確認する。他人の会話では試験しない。
7. 試験終了後はLINEの試験用2設定を外し、個人メールの自動返信を無効化するか連携解除する。業務用への切替は [アカウント移行メモ](personal_to_business_accounts_2026-09-26.md) に従う。

## 検証

- 変更前：関連バックエンド54件、LINE会社プラン7件、旧営業3経路成功。
- 変更後：関連バックエンド58件、画面17件、本人限定LINE9件成功。外部送受信・実課金はなし。
- 開発DBで0025→0026の適用成功。APIで新規既定値・既存値の維持・切替時の受信位置初期化・不正な検索文字列の拒否を確認。
- Chromeの実ブラウザー操作は前回request-header-policy読込失敗で停止した状態。今回の画面17件はローカルの架空データによる回帰テストで、本番の画面操作ではない。

## 本番反映記録（2026-09-26 20:29 JST）

- 対象コミット `cb4d945`。`scripts/deploy_customer_scope_20260926.py` とGitから取り出した6ファイルのハッシュ付きJSONを、VPSの `/home/ubuntu/deploy-customer-scope-20260926` に転送した。SSHの通常ubuntuユーザーと既存Docker管理権限を使用。
- 本番ホスト・API・schedulerの変更前ハッシュ、DB0025、処理中依頼0、稼働メールアカウント0を確認。DBのカスタム形式dumpを作成してpg_restoreの一覧検証を行い、変更対象の旧ソースを個別退避した。
- 退避先 `/home/ubuntu/deploy-customer-scope-20260926/backup-20260926_202915`。DB・旧ソースは700の退避ディレクトリ内、ファイルは600。秘密ENVはコピーも更新もしていない。
- ホストの書込マウントは `src`・`frontend`・`migrations` だけ。ネットワークなし・読み取り専用ルートFS・capabilityなし・no-new-privilegesの一回限りコンテナで、許可した6ファイルを原子的に更新。ホスト全体・Docker socket・ENV・workspaceはマウントしない。
- 稼働APIとschedulerの同じ6ファイルをappuser権限で更新し、0026を適用後に両プロセスを再起動した。ENV変更なし、コンテナID同一、DBの起動日時不変。稼働コンテナの元イメージIDは同じだが、実行中のファイルと、次回再作成用の両イメージ・ホストのビルド元は同じ新コード。
- API再作成用 `tsunagumo-customer-api:20260926_202915` → `docker-api:latest`、scheduler再作成用 `tsunagumo-customer-scheduler:20260926_202915` → `docker-scheduler:latest`。両イメージは既存ローカルイメージをベースにネットワークなしで6ファイルだけを差し替えた。
- 切戻し用タグは `tsunagumo-customer-rollback-api:20260926_202915` と `tsunagumo-customer-rollback-scheduler:20260926_202915`。コード切戻しは退避した `host.json`・各コンテナのJSONから元の5ファイルを同じ限定書込で戻し、両タグを戻して両プロセスを再起動する。追加列は旧コードと互換のため0026を維持し、DB全体を古いdumpに戻さない。
- 公開アプリ200・更新HTMLのハッシュ一致、API起動完了、scheduler起動完了と定期スキャン実行、両方の起動後ERROR/Traceback 0件を確認した。
- LINE Workerは `wrangler deploy --dry-run --keep-vars` の成功後、`--keep-vars` で反映。新バージョン `e3a576c0-0d4f-48bb-8de8-b814505c1be3`、切戻し元 `6eda50df-2902-4d4c-997f-c5dd897e975e`。本人userId用secretは未設定で、テスト転送は無効のまま。既存secret・営業変数を維持した。
- LINE公式APIで `@787dbfal` と既存営業Webhookの一致・有効状態を再確認。Workerの予約ページはPCのcurlとVPSで200、無効署名のWebhookは401。Python urllibからの予約ページ確認は403だったが、curlとVPSからは正常で、画面操作をしたとは扱わない。
- 初回イメージ作成はDockerfileのFROMに生のイメージIDを指定して失敗。本番ファイル変更前のため影響なし。既存イメージへローカルタグを付けて再実行し、上記の成功を確認した。

残りは本人のGmailアプリパスワード入力、本人のLINE userId確認・設定、対象会社へのLINEチャネル登録、本人限定の実送受信。ブラウザー接続は今回もrequest-header-policyエラーのため、本人の認証画面を操作できていない。

## メール設定の入力支援・日本語エラー（2026-09-26 20:54 JST）

本人の画面で、ユーザー名が会社名、IMAP/SMTPが空欄、エラーが `[object Object],[object Object]` と表示されていることを確認。FastAPIのdetail配列をErrorへ直接渡す実装が原因だった。共通API・認証・アップロード経路に日本語のエラー整形を追加し、項目名と修正案内を表示する。入力値・パスワード・内部の検証情報は表示しない。Gmailのユーザー名・IMAP・SMTP・ポートを入力するボタンも追加。ボタンは入力だけを行い、保存・接続・送信を行わず、パスワードと件名制限を維持する。

変更前17件・修正後19件の画面テストに成功。本人が開いていた画面には件名制限の欄がなかったため、ページ再読み込み後に新しいボタンと `[TSUNAGUMO-TEST]` を確認して保存する。未保存のパスワードは再読み込みで失われるので本人が再入力する。

`scripts/deploy_mail_ui_20260926.sh` で本番frontendだけを反映し、ホスト・稼働API・再作成用APIイメージ・公開HTMLのSHA-256が `8c81090e9d6f50fdc2f90697f0bb82dc618554ece63aa1a8d2e3488eee704a15` と一致した。再作成用イメージは件名制限のバックエンドを含む既存 `docker-api:latest` をベースに作成しており、旧バックエンドに戻していない。DB・ENV・schedulerコードは変更せず、コンテナID・起動状態・再起動回数不変。

退避先 `/home/ubuntu/deploy-mail-errors-20260926/backup-20260926_205441`。切戻しイメージ `tsunagumo-mail-errors-rollback:20260926_205441`、新イメージ `tsunagumo-mail-errors-ui:20260926_205441`。実Gmailの設定保存・ログイン・送信はまだ実施していない。
