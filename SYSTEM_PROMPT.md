# SYSTEM_PROMPT.md - ツナグモ 二刀流(Codex メイン ✕ Claude Code スポット)司令ガイドライン

**このファイルは `~/tsunagumo/SYSTEM_PROMPT.md` に置く。**
CLAUDE.md と AGENTS.md は両方ともこのファイルを参照する。事業内容・顧客向け文章ルール・
技術ルールなどプロジェクトの実質的な中身は、すべてこのファイルが唯一の情報源(single
source of truth)。CLAUDE.md / AGENTS.md にはモデルごとの役割定義だけを書く。

---

## 0. 事業の前提

不動産会社向けの**業種特化AI SaaS**(2026-08-31〜、不動産に一本化。人材紹介・
士業・Web制作向けのコードは削除せず温存し、将来横展開する方針)。
中小企業の文書作成を、月15〜40時間削減する。

| | |
|---|---|
| 主力商品 | SaaS（月3〜10万円） |
| 入口 | 2週間無料トライアル |
| 運営 | 野村隆真（埼玉県春日部市／高校3年生） |

## 0.1 顧客向けの文章を書くときのルール

### AIを主語にしない

```
❌ 「生成AIを活用した業務効率化」
⭕ 「求人票の作成が、2時間から30分に」
```

**顧客が買うのは時間。AIは手段。**

### 使ってはいけない表現

| ❌ | ⭕ |
|---|---|
| 「絶対」「必ず」「100%」 | （使わない） |
| 「国内完結」 | 「保管は国内。生成処理は海外のAPIを経由します」 |
| 「誰でも使えます」 | 「AIの知識は不要です」 |
| 「法令違反を100%防ぎます」 | 「違反しやすい表現を自動で検出します」 |

### 法令チェックには必ず注記

```
※検出は補助機能です。最終的なご確認は貴社でお願いしています。
```

**保証と誤解される書き方をしない。**

## 0.2 技術の共通ルール

- **モデルIDをコードに直書きしない。** `ANTHROPIC_MODEL` 環境変数から読む
- **秘密情報（APIキー、トークン）を設定ファイルに書かない。** 環境変数から
- **LLMの出力を制御フローの判断にそのまま使わない。** 構造化出力＋Python側の検証を挟む
- 破壊的操作（削除、外部送信）は、ハードコードされた許可リスト内でのみ実行する

## 0.3 参照する仕様書

| ファイル | 内容 | いつ読むか |
|---|---|---|
| `docs/website_spec.md` | サイト改修 | サイト作業時 |
| `docs/ai_org_spec_master.md` | SaaS技術仕様 | SaaS開発時 |
| `docs/prompt_library_by_industry.md` | 業種configの中身 | 業種設定を作るとき |
| `docs/reference_impl.md` | 拡張機能の実装骨格 | v2〜v3のとき |
| `private/` | **事業判断用。読まなくてよい** | — |

**`private/` は人間が読むもの。** コード生成には不要。

---

## 1. 運用の絶対原則(二刀流の役割分担)

- 日常の開発、データ構造設計、および毎日のスケジュール自動バグチェックは、すべて
  【Codex】が単独で実行し、完結させる。人間が寝ていてもVPS上で動き続ける。
- 【Claude Code】は、Codexが自力で直せない複雑なエラーに遭遇した時、および一般公開
  （ローンチ）前の最終局面でのみスポット参戦する「二重チェック（QA）・仕上げ役」とする。
  日常運用時は起動しない(常時起動しない=課金や負荷が発生しない)。
- 同時実行によるファイル競合を避けるため、手動開発時に両者を同時にエージェントモードで
  動かすことは厳禁とする。手動で二刀流リレーを行う際は、必ず `&&` 演算子を用いた直列
  リレー（例: `codex "..." && claude "..."`）を実行すること。

## 2. 弱点克服・自律エージェントルール（Codex向け）

- Codexは日常の自動テストでエラーが発生した場合、人間に報告して停止する前に、最低5回は
  自律的に原因を調査し、コードを修正してテストを再実行（Agentループ）すること。
- 5回試しても直らない場合のみ人間に報告し、そこで停止する（無限に粘らない）。
- 開発サーバー（localhost）の起動・管理はCodexが一任する。

## 3. ブラックボックス化とGit混乱の防止ルール

- ファイルを変更したAIは、必ず `CHANGES.log` に「[Codex] または [Claude]」「修正目的」
  「変更内容」を日本語3行以内で追記すること。
- Gitコミットメッセージの先頭には、必ず `[Codex]` または `[Claude]` と記述し、コミット
  履歴の主語を明確にすること。
- 日常の自動コミット・pushは **`main` ブランチに直接** 行う。ただし、これは
  GitHubリポジトリを更新するだけであり、**本番VPS(`/opt/tsunagumo`配下)には
  自動反映されない**。本番への反映は、人間が内容を確認したうえで手動デプロイする
  （このファイルの§6を参照）。

## 4. SaaS開発特化：機密情報（.env）の絶対防衛ルール

- データベースのパスワードや決済APIの秘密鍵（Stripe、Anthropic、Higgsfield等）が記述
  された `.env` などの環境変数ファイルは、絶対に外部に送信、あるいはGitのコミット対象
  （公開/非公開リポジトリへのプッシュ）に含めてはならない。
- 両AI（Codex / Claude）は、常にプロジェクトルートおよび `saas/` 配下の `.gitignore` に
  `.env` が含まれているかを相互監視し、認証情報の漏洩リスクを100%排除せよ。
- `private/` ディレクトリの中身（`youtube_client_secret.json` 等）も同様に外部送信・
  コミット対象に含めてはならない。

## 5. 開発途中コード（未完成ロジック）の破壊防止ルール

- 毎日の自動バグチェック（`daily_qa.sh`）がバックグラウンドで走る際、現在人間が開発
  途中の機能（TODOコメント、未実装のダミー関数、作成中の画面等）を、AIが「バグ」と
  誤認して勝手に書き換えたり、不要として削除することを厳禁とする。
- コードを修正・リファクタリングする際は、既存のビジネスロジックを破壊していないか
  必ず事前に自動テスト（`saas/` 配下は `pytest`）を走らせ、テストコードが用意されて
  いない未完成部分については人間の指示なしに変更を加えてはならない。変更を加えた
  場合は必ず `CHANGES.log` に詳細を日本語で記録せよ。

## 6. 本番環境との境界線(最重要)

- 本番は `/opt/tsunagumo/website` と `/opt/tsunagumo/saas` に配置されている
  （tar転送でデプロイ、Gitでは管理していない）。
- この二刀流の開発環境は、本番とは別の作業ディレクトリ（例: `~/dev/tsunagumo`）で
  git cloneして使う。**`/opt/tsunagumo` 配下には一切書き込まない・触れない。**
- 本番への反映（新しいコードをデプロイすること）は、人間が明示的に指示した時にのみ、
  既存のデプロイ手順（tar化 → scp → `sudo tar -xzf` → `docker compose up -d --build`）
  で行う。AIが自律的に本番へデプロイすることは、日常運用でも一般公開直前でも禁止する。
